# 复制本文件为 .env，并把 your_password 改成你的 MySQL root 密码
DATABASE_URL=mysql+pymysql://root:your_password@localhost:3306/lost_and_found

# JWT 密钥：请改成足够长的随机字符串（生产环境务必保密）
JWT_SECRET_KEY=change-me-to-a-long-random-secret
# 令牌有效期（分钟），默认 1440 = 24 小时
JWT_EXPIRE_MINUTES=1440